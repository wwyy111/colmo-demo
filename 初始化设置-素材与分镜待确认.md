# 初始化设置：本版实现与素材清单

## 已实现

本轮已获准实际制作。初始化合并为“制定并启用唤醒方案”，包含表达需求、推荐方案、确认体验、反馈与确认四个时间点。其他交互保留原拆解信息，不宣称已完成全部场景。

- 全景标注灯光、窗帘、音箱、智能床、毫米波雷达、4寸中控屏和 App，设备名直接放在设备附近。
- 主线依次体验灯光渐亮与受限窗帘开启。音箱静默、智能床不动作，手表仅作已授权且可用的备用说明。
- 用户气泡、字幕、需求摘要、策略卡与模型镜头联动。
- Dynamic Line、Layered Glass 均实现待机、聆听、整理判断、说明、等待、执行、完成状态。
- 20句本地合成语音、4类原创程序合成提示音，已按逐句时长接入。
- 播放、暂停、重播、进度拖动、节点标记和声音开关；默认有声。浏览器禁止自动有声播放时暂停，并通过声音按钮继续。
- 操作页面会暂停自动播放；场景内滚轮只缩放，右侧滚动只浏览内容。非交互区下滑可打开场景栏。
- 卡片采用响应式组件，场景内等比适配，不加右侧黑色遮罩；右侧保留可阅读的完整参考卡与操作。
- 可选再次体验默认包含，可取消勾选。反馈仅调整灯光亮度与渐亮节奏，窗帘安排不变。

## 已落地分镜

| 顺序 | 内容 | AI与设备表达 |
| --- | --- | --- |
| 1 | 设备全景 → 用户走向中控屏 | 先交代设备，再推近用户，不提前生成方案 |
| 2 | 用户自然语言描述 | 明早七点、灯光优先、窗帘开一点、伴侣免扰；管家聆听 |
| 3 | 整理需求 → 推荐方案 | 摘要转策略，解释声音免扰与备用条件，草案未启用 |
| 4 | 用户选择体验 → 设备检查 | 用户同意后开始，不把体验视为正式启用 |
| 5 | 灯光渐亮 → 窗帘受限开启 | 分别聚焦两种物理变化，普通检查不逐台播报 |
| 6 | 环境恢复 → 用户反馈 | 用户提出灯光调暗、放慢，窗帘保持 |
| 7 | 更新方案 | 明确本次修改与保持不变的项 |
| 8 | 可选再次体验 | 灯光更柔和、更慢；窗帘沿用原定开度与节奏 |
| 9 | 确认 → 检查保存 → 启用结果 | 完成音只用于检查保存成功的演示结果 |

默认完整主线约106秒（包含再次体验）。每段时长取自音频清单，非固定猜测。

## 异常和快捷路径

- 正常主线不强行串入异常；通过每个时间点的“情况演示”切换。短暂闪白区分案例，减少动态效果模式下关闭闪白。
- 表达需求：日期不明确才追问；补充后只保留用户实际表达的信息。
- 推荐方案：备用偏好、免扰冲突、设备不可用三项合并一张卡。异常短音只在案例开头响一次。
- 体验：灯光未响应时灯具不假装变亮，保留未完成状态。
- 启用：失败时停在尚未启用，不播放完成音。
- 异常条件未解决时不允许通过“确认启用”跳成成功；重试仍展示对应未完成示例。
- 可直接确认跳过体验，但仍经过检查保存的演示节点。
- 可中止体验、停用结果，或调整灯光与窗帘查看自定义体验；自定义是独立预览，返回主线恢复预设。

## 素材来源与待补内容

| 内容 | 当前可用版本 | 后续可补充 |
| --- | --- | --- |
| AI与用户语音 | macOS本地合成音：Tingting / Eddy 中文；逐句 MP3 | 品牌最终音色，保持文件与文本对应 |
| 短音效 | 进入、生成、完成、异常四类原创程序合成 WAV | 品牌声音规范及最终声音资产 |
| 两套管家 | 可暂停的实时 CSS 动效，全部语义状态 | 最终品牌动效源稿或透明视频；当前无需另补即可演示 |
| 信息卡片 | 响应式网页组件，包含正常、异常、修改、成功和失败 | 如需设计师定稿，可提供同状态 SVG / 设计源稿 |
| 模型分镜 | 实时3D镜头、用户行走、灯光及窗帘变化 | 正式设备模型、灯光强度与开度范围、雷达能力需产品确认 |

页面“素材清单”入口已标注可替换项；“听觉”页可逐句试听。

全部20句文本、角色、文件、时长、合成来源记录在 `ai-1/media/setup/manifest.json`。文案源为 `ai-1/setup-dialogue.js`；生成脚本为 `build-setup-media.mjs`，支持按句重新生成。不要将整条流程混音，否则无法正确暂停、跳转和替换。

## 验证与交付边界

- 原有回归测试与新增初始化测试通过。
- 浏览器验证了全部镜头、声音映射、暂停恢复、静音记忆、形象切换、异常限制、自定义体验、滚轮隔离和390/1292/1920宽度布局。
- 完整正常主线连续播放到“已启用”，无脚本错误，默认声音未静音，结束正常停止。
- 本平台是交互设计演示，所有设备回执和启用结果为预置模拟，不连接真实设备，不代表真实AI推理或唤醒效果。
- 本轮只同步 Git 和本地预览，不发布线上。
