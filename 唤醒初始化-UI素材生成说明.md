# 唤醒初始化：UI 素材生成说明

## 交付范围

设计一类「唤醒方案卡」和一类「状态反馈控件」。本版共 7 张状态示例图：5 张方案卡、2 张反馈控件。取消原因尚未定义清楚的「启用失败」示例，不再生成该张图。本文仅调整素材说明，不代表网页已同步删除该分支。

7 张是设计状态参考，不是 7 套独立组件；网页应复用组件更新文字和状态。页面栏目建议由「信息卡片」改为「信息呈现」。

建议先完成图 2 和图 7 两张母版，确认样式后再制作其他状态。优先交付可编辑设计源文件与 SVG；文字保持可编辑，不转曲更利于网页实现。另附 PNG 预览。卡片尺寸一致、允许按内容自适应高度，不将整个页面或人物背景烘焙进素材。

## 每张图共用的生成描述

为智能家居中控屏设计唤醒设置界面组件。风格简洁、温和、克制，浅色背景、深色文字、低饱和绿色强调色，统一圆角、字体、间距和按钮样式。异常状态使用少量琥珀色，不把整个组件染成警告色。所有状态保持相同宽度与对齐关系，不加入人物、AI 管家形象、场景模型或装饰插画。文字清楚可读，避免大面积留白和过多说明。一个区域最多一个主按钮，其他操作降低强调。沿用已确认母版，不因状态变化重新设计布局。

## 两类组件的区别

### 唤醒方案卡

回答「AI 给我安排了什么？」固定标题「唤醒方案」，包含状态标签、时间与执行日期、设备安排、必要说明及操作区。需求整理、推荐、调整和启用均复用这一结构。尚未明确的信息不得擅自填入，修改后保留完整方案并局部突出变化。

### 状态反馈控件

回答「现在进行到哪一步，有什么需要处理？」采用紧凑的横向状态条，不做另一张大方案卡。正常状态一两行，异常才展开原因与操作。异常不能自动消失。体验中的设备状态优先在模型设备旁呈现。

## 图 1：唤醒方案卡／需求整理

出现位置：用户表达需求之后，AI 正在理解。

生成描述：设计标题为「唤醒方案」的信息卡，右上角状态「整理中」。突出「07:00」，下方「明天 · 仅本次」。正文列出用户已表达的需求：「优先灯光唤醒」「窗帘开一点」「尽量不打扰伴侣」。底部用轻量状态文字「正在整理设备安排」。不出现确认启用按钮，不提前将手表等尚未推荐的方式写成用户需求。与图 2 使用相同的标题区和时间区。

## 图 2：唤醒方案卡／待确认【方案卡母版】

出现位置：AI 生成推荐方案。

生成描述：标题「唤醒方案」，状态「待确认」。时间「07:00」，执行日期「明天 · 仅本次」。正文按顺序展示「1 灯光柔和渐亮」「2 窗帘小幅开启」。下方单独列出「备用方式：必要时使用已授权手表轻振」。补充「卧室音箱不参与，减少对伴侣的干扰」。备用手表与主流程分开，不画成必然执行的第三步。底部主按钮「确认启用」、次按钮「体验一下」、文字操作「调整方案」。

## 图 3：状态反馈控件／体验准备

出现位置：用户选择体验，AI 开始检查设备。

生成描述：一个紧凑的横向状态反馈控件。左侧小型进度指示，主文字「正在检查体验设备」，次文字「灯光、窗帘」，右侧低强调操作「取消」。最多两行信息，不出现起床时间、完整方案或大标题。控件高度明显小于方案卡，保持相同视觉语言。

复用方式：启用保存过程中仅将文字改为「正在启用方案」，无需再生成一种版式。此阶段不默认提供无法真正取消的操作。

## 图 4：唤醒方案卡／已调整

出现位置：用户反馈亮度和节奏不合适，AI 完成修改。

生成描述：完整复用图 2，标题仍为「唤醒方案」，状态「已调整 · 待确认」。保留时间、执行日期、窗帘及备用方式的完整内容。只在灯光行增加浅绿色局部强调和「已调整」标记，内容改为「亮度降低，渐亮速度放慢」。其他行保持原样，不额外增加「保持不变」大段说明。底部主按钮「确认启用」、次按钮「再次体验」、文字操作「继续调整」。

## 图 5：唤醒方案卡／已启用

出现位置：计划保存并启用成功。

生成描述：完整复用图 4 的方案内容。标题「唤醒方案」，右上角绿色完成标记与「已启用」。保留「07:00／明天 · 仅本次」和最终确认的灯光、窗帘、备用安排。去掉灯光行修改高亮。操作改为「调整方案」和低强调的「停用」。不要将完整方案替换为单纯成功通知。

## 图 6：唤醒方案卡／条件待处理

出现位置：推荐方案存在多项条件问题；属于异常分支，并非每次正常设置都会出现。

生成描述：复用图 2，状态「待处理 · 3 项」。保留已确定的时间及可用的灯光、窗帘安排。同一卡内集中呈现「待处理事项」：①「备用偏好未明确：请选择是否需要其他唤醒方式」；②「音箱与免扰要求冲突：暂不加入方案」；③「手表暂不可用：检查连接或选择其他方式」。问题采用少量琥珀色，不拆为三张卡。底部主操作「处理问题」、次操作「体验可用设备」，不提供可点击的「确认启用」。

说明：本异常示例存在相互关联的备用方案问题，生产版本需由产品侧确认实际校验条件。用户移除不可用的备用方式后，应允许按重新确认的可行方案继续，不能无限阻断。

复用方式：若只缺执行日期，在日期位置显示「待确认」及「仅明天／每个工作日」选项。无需另做「执行日期已补充」卡，补充后直接更新原卡。

## 图 7：状态反馈控件／体验异常【异常反馈母版】

出现位置：灯光体验已发起，但灯光没有响应。

生成描述：一个持续显示的轻量异常反馈条，左侧琥珀色提示图标。主文字「灯光未响应」，次文字「本次灯光体验未完成」。提供主操作「重新检查」、次操作「先体验窗帘」，并保留低强调的「结束体验」。不包含起床时间、完整设备清单或大面积警告背景。没有检测依据时不写死「设备离线」或「网络故障」。

行为前提：「先体验窗帘」只在窗帘可用且用户同意时执行；灯光保持「未完成」，不能因为跳过就显示全部体验成功。若窗帘也不可用，则不提供该操作。

## 不用单独生成的画面

- 灯光体验、窗帘体验：在模型设备旁使用「渐亮中」「开启中」「已完成」小状态标。
- 再次体验：复用状态标，必要时加「按调整后参数体验」。
- 体验结束：回到原方案卡，由字幕或管家询问感受，不另做大卡。
- 体验停止：轻量提示「体验已停止」，回到原方案卡。
- 方案停用：原方案卡状态改为「已停用」，保留内容，操作调整为「重新启用」「调整方案」。
- 启用失败：本轮暂不生成。其触发机制未定义清楚，不能混同计划保存与设备执行。

## 与场景内容分工

AI 管家形象负责回应状态；方案卡展示用户可确认的安排；设备旁状态标展示正在发生的动作；场景说明文字用于向评审解释 AI 的处理策略。不要把四种信息重复写在每一张图上。

## 交付核对

1. 五张方案卡看得出是同一份方案的状态演进。
2. 两张反馈控件明显比方案卡轻，不遮住主要模型动作。
3. 不以颜色作为唯一状态依据，始终有明确文字。
4. 异常不伪装成功，缺失的信息不擅自补齐。
5. 输出源文件／SVG 与 PNG 预览；可编辑文字需逐字核对，特别是 AI 图像生成后的中文。
